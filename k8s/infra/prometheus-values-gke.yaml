# GKE-specific prometheus values
# Only contains differences from prometheus-values.yaml (base)

grafana:
  # Ingress disabled - use port-forward instead: make gke/grafana
  ingress:
    enabled: false

  # ClusterIP service
  service:
    type: ClusterIP
    port: 80
