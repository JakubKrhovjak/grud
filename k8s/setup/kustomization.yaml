# Setup kustomization - includes operator installation
# Usage: kubectl apply -k k8s/setup/
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # CloudNativePG operator from remote URL
  - https://raw.githubusercontent.com/cloudnative-pg/cloudnative-pg/release-1.22/releases/cnpg-1.22.0.yaml
