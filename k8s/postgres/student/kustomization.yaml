# Student database overlay
# Uses base PostgreSQL templates
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: grud

# Use base PostgreSQL templates
resources:
  - ../../base/statefulset-postgres.yaml
  - ../../base/service-postgres.yaml

# Name prefix
namePrefix: student-

# Common labels
commonLabels:
  app: student-db
  app.kubernetes.io/name: student-db
  app.kubernetes.io/component: database

# Patches for student-specific values
patches:
  # Update secret references
  - target:
      kind: StatefulSet
      name: db
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/env/0/valueFrom/secretKeyRef/name
        value: student-db-app
      - op: replace
        path: /spec/template/spec/containers/0/env/1/valueFrom/secretKeyRef/name
        value: student-db-app
      - op: replace
        path: /spec/template/spec/containers/0/env/2/valueFrom/secretKeyRef/name
        value: student-db-app
