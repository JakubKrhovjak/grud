# Kind cluster values
global:
  environment: kind
  namespace: grud

# Secret management
secrets:
  # For Kind: create simple Kubernetes Secrets
  createKubernetesSecrets: true
  useExternalSecrets: false

  # Default values for Kind (will be auto-generated if empty)
  jwtSecret: ""  # Leave empty to auto-generate

databases:
  student:
    username: app
    password: ""  # Leave empty to auto-generate
    database: university
    host: student-db-rw
    port: 5432

  project:
    username: app
    password: ""  # Leave empty to auto-generate
    database: projects
    host: project-db-rw
    port: 5432

studentService:
  enabled: true
  replicaCount: 2
  image:
    repository: localhost:5001/student-service
    tag: latest
  config:
    env: kind
  # Use secrets for sensitive data
  useSecrets: true

projectService:
  enabled: true
  replicaCount: 2
  image:
    repository: localhost:5001/project-service
    tag: latest
  config:
    env: kind
  useSecrets: true

adminPanel:
  image:
    repository: localhost:5001/admin-panel
    tag: latest
  config:
    apiUrl: "http://localhost:8080"
  enabled: true
  replicaCount: 2

studentDb:
  enabled: true
  replicaCount: 1
  nodeSelector:
    node-type: db
  tolerations:
    - key: workload
      operator: Equal
      value: db
      effect: NoSchedule

projectDb:
  enabled: true
  replicaCount: 1
  nodeSelector:
    node-type: db
  tolerations:
    - key: workload
      operator: Equal
      value: db
      effect: NoSchedule
