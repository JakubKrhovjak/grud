# Student Service - Local Development (IDE)
env: local

server:
  port: "9080"
  read_timeout_seconds: 30
  write_timeout_seconds: 30
  idle_timeout_seconds: 120
  cors_origins:
    - "http://localhost:5173"
    - "http://localhost:3000"

database:
  host: localhost
  port: "5433"
  user: app
  password: changeme123
  name: university
  ssl_mode: disable
  max_open_conns: 25
  max_idle_conns: 10
  conn_max_lifetime_seconds: 300
  conn_max_idle_time_seconds: 60

project_service:
  grpc: localhost:50052

nats:
  url: nats://localhost:4222
  subject: student.messages
